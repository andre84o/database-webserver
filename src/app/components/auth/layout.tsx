const AuthLayout = ({ 
    children }: { children: React.ReactNode }) => {
    return (
            <>
            
            <h1>Auth</h1>
            {children}
        
        </>
    )
}

export default AuthLayout
